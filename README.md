Trend Insight SPA (NewTrand)

โปรเจคสำหรับอ่านข้อมูลเทรนด์จาก Trend Digest และ knowledge/SOUL แล้วนำมาจัดแสดงบนหน้าแดชบอร์ดแบบ SPA (Single Page Application)

โครงสร้างโปรเจค:
- frontend/ : ไฟล์ส่วนหน้า (HTML/CSS/JS)
- data/     : ข้อมูลเทรนด์ตัวอย่าง (mock data)
- server_run.sh : สคริปต์รันเซิร์ฟเวอร์แบบ local

แนวทางใช้งาน:
1) เปิดเซิร์ฟเวอร์ด้วย: ./server_run.sh
2) เปิดเบราว์เซอร์ที่ http://127.0.0.1:8000/
3) หน้าแดชบอร์ดจะแสดงข้อมูลเทรนด์วันนี้ โดยข้อมูลมาจาก data/trends.json

หมายเหตุ: ในอนาคตจะเชื่อมต่อกับ Trend Digest จริง และ Live feed สำหรับอัปเดตข้อมูลอัตโนมัติ

"}```
# NewTrand Mobile App
